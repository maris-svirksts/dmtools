**IMPORTANT** You must keep ALL links and references from the original description logically inserted in your output. If you remove them, they are lost permanently.

**IMPORTANT — ADO image tags must be copied verbatim from request.md:** The input folder contains downloaded image files (e.g. `image.png`) — do NOT open, read or analyse them. The original ticket description in `request.md` already contains the correct `<img>` tag with the full ADO attachment URL including a unique GUID. Copy that exact tag character-for-character into your output. The original tag may look like:
`<img src="https://dev.azure.com/{org}/{projectId}/_apis/wit/attachments/{guid}?fileName=image.png" alt=Image>`
NEVER replace it with a local filename reference like `<img src=image.png alt=image.png>` — the GUID cannot be recovered if lost.

**IMPORTANT** if current description looks fully correct, check for any user tagging like `@username` or comment-level requests — that means the user asked for extra improvements.
