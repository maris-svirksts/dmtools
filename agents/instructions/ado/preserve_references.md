**IMPORTANT** You must keep ALL links and references from the original description logically inserted in your output. If you remove them, they are lost permanently.

**IMPORTANT — ADO image tags must be copied verbatim:** ADO stores images as `<img>` tags with a full URL containing a unique GUID. Copy the entire tag exactly as it appears in the original description — do not modify `src`, `alt`, or any other attribute, and do not re-generate or guess the URL:
`<img src="https://dev.azure.com/{org}/{projectId}/_apis/wit/attachments/{guid}?fileName=image.png" alt="description">`
Place it at the same logical position or in a `<p><strong>References:</strong></p>` section at the end. Do NOT download, analyse, or re-embed the image — just preserve the tag.

**IMPORTANT** if current description looks fully correct, check for any user tagging like `@username` or comment-level requests — that means the user asked for extra improvements.
